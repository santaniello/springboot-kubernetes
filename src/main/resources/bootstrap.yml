spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: default
            name: springboot-kubernetes
      reload:
        enabled: true
        mode: event
        strategy: refresh